## Branding update

1. Copy the uploaded logo to `src/assets/tkm-logo.png`.
2. In `src/routes/index.tsx`:
   - Import the logo asset.
   - Replace the header on both the form view and the success view with the logo (centered, ~96px tall) above a new title: **"Toloba ul Kulliyaat il Mumenoon — Miqaat/Event Photos"**.
   - Keep existing subtitle/description text.
   - Update `head()` meta title to `"TKM Miqaat / Event Photos Submission"` and refresh the description accordingly.
